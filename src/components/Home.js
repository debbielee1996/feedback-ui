import { Typography } from "@mui/material"

const Home = () => {
    return (
        <div>
            <Typography variant="h3">
                Welcome to the Feedback app!
            </Typography>
        </div>
    )
}

export default Home
